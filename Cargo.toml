[package]
name = "goog"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.2"
termion = "4.0.6"
tokio = { version = "1.53.1", features = [
  "sync",
  "rt",
  "macros",
  "rt-multi-thread",
  "time",
] }
